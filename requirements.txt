# rotquant-eval requirements.
#
# Pin every version that installs cleanly into requirements.lock; this corner of
# the ecosystem moves fast and breaks often. Record torch.__version__, CUDA,
# driver, and GPU in every result file (rotquant.utils.environment_record does this).
#
# Core (CPU foundation + tests run with just these):
torch>=2.4
numpy
scipy

# Models / data / eval (GPU run):
transformers
datasets
accelerate
safetensors
sentencepiece
pandas
matplotlib
pyyaml
pytest

# Fast Hadamard (the rotation primitive QuaRot/QuIP# use). CUDA-only build;
# rotquant.rotate falls back to a pure-torch FWHT on CPU.
fast-hadamard-transform

# Evaluation harness:
lm-eval

# Baselines (install lazily as needed):
gptqmodel        # GPTQ (auto-gptq successor)
autoawq          # AWQ
aqlm[gpu]        # AQLM 2-bit vector codebooks
flute-kernel     # runtime for HIGGS (rotation + MSE-optimal grid)
# QuIP# and QTIP are repos, not packages -- clone:
#   git clone https://github.com/Cornell-RelaxML/quip-sharp
#   git clone https://github.com/Cornell-RelaxML/qtip
